# Add inside the existing zscape.zeroshot.bio server block (alongside /minifin/):
location /autopilot/ {
    proxy_pass http://127.0.0.1:5008/;
    proxy_http_version 1.1;
    proxy_read_timeout 120s;
    proxy_set_header Host $host;
}
